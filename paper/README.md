# Paper (LaTeX)

This folder contains the Elsevier-style LaTeX sources of the DAGFS manuscript.

## Build

```bash
cd paper
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

## Notes

- The compiled artifacts (`main.pdf`, `.aux`, `.log`, …) are ignored by `.gitignore`.
- `figures/` and `tables/` contain the final PDFs/Tables used by the manuscript.
  They can be regenerated by running the experiment scripts (data/results not shipped in this repo).

